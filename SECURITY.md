# 安全策略

## 支持的版本

| 版本 | 安全更新 |
|---|---|
| 1.0.x | 是 |
| < 1.0 | 否（请升级到最新 Release） |

## 漏洞报告

**请不要通过公开 Issue 报告安全漏洞。**

请使用 GitHub 私密漏洞报告：

**[Report a vulnerability](https://github.com/taiwanli/LocalDM/security/advisories/new)**

报告中请尽量包含：

1. 受影响的版本 / commit
2. 漏洞类型（如本地 API 未授权访问、路径穿越、命令注入等）
3. 复现步骤或最小验证代码
4. 影响范围与你认为的严重程度
5. 若有缓解思路，可一并说明

## 处理流程

1. 维护者会在合理时间内确认收到报告。
2. 复现与评估后，会与你沟通修复方案与时间表。
3. 修复发布后，可在协商下致谢（默认不公开你的身份）。

## 安全相关设计说明

使用或二次开发时请注意：

- 本地 HTTP API 默认仅绑定 `127.0.0.1`，并校验 `api-token`。
- API token 保存在本机 `userData/LocalDM/api-token`，**请勿提交仓库、截图或聊天记录**。
- 浏览器扩展仅向本机回环地址发送 capture 请求。
- 请仅下载你有权获取的文件；本项目不提供任何绕过付费墙或授权的机制。
- 外部工具（yt-dlp / ffmpeg / aria2c）请从官方或可信渠道获取，并关注其自身安全公告。

## 范围外

以下通常不在本项目安全支持范围：

- 用户自行修改源码后的部署
- 未使用官方 Release / 本仓库源码构建的第三方打包
- 操作系统、驱动、网络环境导致的问题
- 对第三方网站或服务的未授权访问尝试
